[build-system]
requires = [
	"setuptools>=42",
	"wheel",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
asyncio_mode = "strict"

[tool.black]
line-length = 150

[tool.pyright]
exclude = [
	"dist/**",
	"build/**",
	"docs/**",
	".tox/**",
]
pythonVersion = "3.7"
